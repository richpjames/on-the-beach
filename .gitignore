node_modules/
dist/
playwright-report/
test-results/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment/local config
.env
.env.*
!.env.example

# SQLite
*.db
*.db-wal
*.db-shm

# OS/editor files
.DS_Store
.idea/
.vscode/

# Git worktrees
.worktrees/

# Eval results (generated, may contain large output)
eval/results/*.json
!eval/results/.gitkeep

# Eval pending review (user data, not for commit)
eval/fixtures/pending-review.json

# Playwright MCP snapshots and screenshots
.playwright-mcp/
*.png

uploads

# SvelteKit
.svelte-kit/
build/

# iOS / Capacitor native shell
# The ios/App project is generated by `npx cap add ios` on a mac and is not
# committed; only the hand-authored ios/ShareExtension sources live in git.
ios/App/
ios/DerivedData/
ios/ShareExtension/Secrets.xcconfig
*.ipa
Pods/
