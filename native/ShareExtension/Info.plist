<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>On The Beach</string>
	<key>CFBundleName</key>
	<string>ShareExtension</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>

	<!-- App configuration. OTBBaseURL is safe to commit; OTBIngestAPIKey is
	     resolved from Secrets.xcconfig at build time (gitignored). -->
	<key>OTBBaseURL</key>
	<string>https://onthebeach.ricojam.es</string>
	<key>OTBIngestAPIKey</key>
	<string>$(OTB_INGEST_API_KEY)</string>

	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- Offer the extension whenever the share payload contains a web
			     URL, text that may embed a URL, or images (shared photos, e.g.
			     record covers). NSExtensionActivationSupportsWebPage is
			     intentionally NOT used: it requires an
			     NSExtensionJavaScriptPreprocessingFile, and without one the whole
			     activation rule is malformed and iOS silently drops the extension
			     from the share sheet. We only need the URL, which WebURL/Text
			     already cover.

			     The image count is what caps a multi-select share from Photos:
			     iOS hides the extension entirely once the selection exceeds it,
			     so 1 meant "one sleeve at a time". Each photo is uploaded and
			     vision-scanned as its own request, so this is a deliberate cap
			     rather than an open door — 10 covers a stack of sleeves without
			     turning one share into a minutes-long queue. -->
			<key>NSExtensionActivationRule</key>
			<dict>
				<key>NSExtensionActivationSupportsWebURLWithMaxCount</key>
				<integer>1</integer>
				<key>NSExtensionActivationSupportsText</key>
				<true/>
				<key>NSExtensionActivationSupportsImageWithMaxCount</key>
				<integer>10</integer>
			</dict>
		</dict>
	</dict>
</dict>
</plist>
